TECHNICAL SPECIFICATION & ARCHITECTUREENGINE ALLOCATION SPECIFICATION FOR ANTIGRAVITY (AG)ARCHITECTURE CORE GOALTo build a fast, standard-compliant, responsive single-page web portfolio under React 18 & TypeScript. All animation controls, dynamic layouts, and background horizontal cloud drift loop vectors must be optimized directly in React state & Framer Motion. Zero server constraints, zero slow database responses.1. THE COMPILATION STEPS & STACKLAYERTECHNOLOGYENGINE UTILIZATION / ADVANTAGEFrameworkReact 18 / Vite + TypeScriptStatic-typing safety for multi-mode toggle structures and state contexts.StylingTailwind CSS EngineAllows rapid pixel rendering, inset borders, and absolute grid coordinates without heavy assets.AnimationsFramer MotionCalculates custom infinite cloud drift offsets, active HUD pointers, and spring bounces.DeploymentVercel / GitHub Pages100% serverless, edge network caching, automatic builds on repository commits.2. CORE STATE CONTEXT: INTERACTION MODESManage mode swapping seamlessly using a top-level React view controller state. Toggle 'isArcadeMode' which swap parent container layouts: Arcade Mode binds dynamic styles and keyboard actions (arrows/WASD key listener to move focus frames on skills and menus). Dashboard Mode strips off complex visual components and renders structured text layouts for direct copy-pasting by HR managers. Both configurations load data directly from a static, optimized configuration array 'projectsData.ts' to avoid double maintenance.3. BACKGROUND FLOATING CLOUD PHYSICS• Render 3-4 separate absolute CSS divs representing pixel-art clouds with random size, scale, and vertically staggered starting coordinates.• Bind infinite horizontal offset loops via Framer Motion. Set speed parameters uniquely per cloud instance to establish a depth parallax effect.• Reset layout position state smoothly once target margin boundaries are exceeded, avoiding visible jump cuts.
